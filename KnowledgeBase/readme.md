# Knowledge Base

## Dependencies
	Boost Library 
	Tensorflow C++ API

## Build and Run
	mkdir build
	cd build
	cmake ..
	make
	./save (to create database and save to file)
	./load (to load database from file and query)
